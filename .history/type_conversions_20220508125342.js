// Type conversion

/**
 * converting to string {String(value)}
 * converting to number {Number(str)}
 *
 * */

// Numeric conversion rule:

/*
* undefined = NaN
* null = 0
* true and false = 1 and 0
* string = when whitespace are removed form start to end and still
empty the result = 0 else NaN

*/

// Boolean Conversion

/**
 * value that are intuitively [empty, 0, an empty string, null, undefined, and NaN ] = false
 * Other values become true.
 * */